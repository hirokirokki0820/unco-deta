<div class="mb-3 md:mt-10 md:w-2/3 md:mx-auto">
  <h2 class="text-3xl mb-3 md:text-center">確認メールの再送信</h2>

  <%= form_with model: @user, url: confirmation_path(resource_name), local: true, class: "bg-white px-6 py-5 rounded-md", data: { turbo_method: :post } do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <%# -------- Eメールアドレス -------- %>
    <div class="mb-3">
      <%= f.label :email, class: "font-semibold block p-1 required" %>
      <div class="relative">
        <div class="flex absolute inset-y-0 left-0 items-center pl-3 pointer-events-none">
          <span class="text-gray-600">
            <%= icon("solid", "envelope") %>
          </span>
        </div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class: "form-input", placeholder: "email@example.com" %>
      </div>
    </div>

    <div class="md:flex items-center md:justify-between">
      <%= f.submit "送信する", class: "btn btn-sky mb-3 md:mb-0" %>
      <div class="md:text-right text-sm">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  <% end %>

</div>
